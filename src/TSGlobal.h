#ifndef _TSGLOBAL_H_
#define _TSGLOBAL_H_

#include <iostream>
#include <vector>
#include <string>
#include <map>
#include <algorithm>
#include <tuple>

using std::cout;
using std::endl;
using std::flush;
using std::string;
using std::vector;
using std::map;
using std::tuple;

typedef unsigned char uchar;
typedef long long longlong;
typedef unsigned long long ulonglong;

#endif
